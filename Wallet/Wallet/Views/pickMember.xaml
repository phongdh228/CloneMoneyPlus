<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             x:Class="Wallet.pickMember"
             Title="Chọn thành viên"
             NavigationPage.HasBackButton="True">
    <ContentPage.Content>
        <StackLayout>
            <ListView x:Name="lstMember" ItemSelected="lstMember_ItemSelected" ItemTapped="lstMember_ItemTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ImageCell ImageSource="{Binding memberImage}" Text="{Binding memberTitle}" TextColor="Black" Tapped="ImageCell_Tapped"/>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>